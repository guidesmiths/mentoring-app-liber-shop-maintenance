import React from 'react';
import { Layout } from '../../common';

const CreateIndexPage = () => {

  return (
    <Layout step={2}>
      create index page
    </Layout>
  );
};

export default CreateIndexPage;